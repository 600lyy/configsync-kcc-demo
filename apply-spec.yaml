applySpecVersion: 1
spec:
  configSync:
    enabled: true
    sourceFormat: unstructured
    syncRepo: https://github.com/600lyy/configsync-kcc-demo
    syncBranch: main
    secretType: token
    policyDir: cluster
  policyController:
    enabled: false 
